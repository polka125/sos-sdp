real n, m1, m2;

function T[1, 17];

if { n >= 0 } => { T(n) >= 1 }
// if { n == 1 } => { T(n) >= 1 }

if { n >= 1; m1 * m1 * m1 * m1 * m1 * m1 == n / 2; m2 * m2 * m2 * m2 * m2 * m2 == n} => { T(m2) >= 7 * T(m1) + 8 * (n / 2) * (n / 2) }
